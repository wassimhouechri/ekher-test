apiVersion: v1
kind: Secret
metadata:
  name: flask-app-secret
  namespace: flask-app
  labels:
    app: flask-app
type: Opaque
# DO NOT hardcode real values here.
# Create the secret manually before deploying:
#
#   kubectl create secret generic flask-app-secret \
#     --from-literal=api-key="$(openssl rand -hex 32)"
#
# The placeholder below is only for documentation purposes.
stringData:
  api-key: "REPLACE_WITH_REAL_SECRET_BEFORE_APPLY"
